import wollok.game.*
import snake.*
import visuales.*

program juego {
	game.title("Snake")
		// tamaño pantalla
	game.height(15)
	game.width(25)
		// fondo
	game.boardGround("background.jpg")
		// tamaño celda
	game.cellSize(32)
	carga.creacion()
		// inicializacion del juego
	game.start()
}
